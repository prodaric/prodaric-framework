# @prodaric/node-canvas

Prodaric Framework — Rete.js v2 canvas widget (model editor).

## Instalación

```bash
npm install @prodaric/node-canvas
```

Requiere configuración de `@prodaric:registry=https://npm.pkg.github.com/` y autenticación en GitHub Packages.
