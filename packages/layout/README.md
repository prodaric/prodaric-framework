# @prodaric/layout

Prodaric Framework — Lumino layout & docking (widget registry, dock host, predefined panels).

## Instalación

```bash
npm install @prodaric/layout
```

Requiere configuración de `@prodaric:registry=https://npm.pkg.github.com/` y autenticación en GitHub Packages.
